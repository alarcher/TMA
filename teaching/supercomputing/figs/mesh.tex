\begin{tikzpicture}
  \foreach \i in {0,...,10} {
    \draw[very thick] (\i,0) -- (\i,10);
    \draw[very thick] (0,\i) -- (10,\i);
  }
  \foreach \i in {0,...,3} {
    \foreach \j in {0,...,3} {
      \draw[thick, darkblue, fill=cadet] (\i,\j) circle (0.1);
    }
  }
\end{tikzpicture}
